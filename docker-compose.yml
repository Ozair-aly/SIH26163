version: '3.8'

services:
  app:
    build: .
    container_name: sih26163-security-platform
    ports:
      - "8000:8000"
    environment:
      - DEMO_MODE=true
    volumes:
      - ./database:/app/database
      - ./reports:/app/reports
    restart: unless-stopped
